import styled from "styled-components";

const Contenido = styled.main`
    width:100%;
    height: 30rem;
    background: var(--Dark-Grey);

`

const Main =() =>{
    return(
        <Contenido>

        </Contenido>
    )
}

export default Main